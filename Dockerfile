# Берем за основу официальный образ Ubuntu
FROM ubuntu:latest

# Устанавливаем все необходимые инструменты: wget для скачивания, git для будущего
RUN apt-get update -y && apt-get install -y wget git

# Копируем наш скрипт запуска (который мы создадим дальше) внутрь контейнера
COPY run.sh /app/run.sh

# Даем этому скрипту права на исполнение
RUN chmod +x /app/run.sh

# Приказ: когда контейнер запустится, выполнить наш скрипт
CMD ["/app/run.sh"]
